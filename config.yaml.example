# SiMing 配置文件
# 复制此文件为 config.yaml 并修改相应配置

# 服务器配置
server:
  # HTTP服务端口
  port: 8080
  # 绑定地址
  bind: "0.0.0.0"

# 数据库配置
database:
  # SQLite数据库文件路径
  path: "data/siming.db"

# 监控配置
monitor:
  # 默认监控cron表达式
  # 每天凌晨2点执行一次示例: 0 0 2 * * ?
  default_cron: "0 0 2 * * ?"

# 大语言模型配置
llm:
  # 是否启用大模型分析
  enabled: true
  # 大模型API端点地址 (OpenAI兼容格式)
  api_base: "https://api.openai.com/v1"
  # 模型名称
  model: "gpt-3.5-turbo"
  # API密钥
  api_key: "your-api-key-here"
  # 分析提示词模板
  prompt_template: |
    你是一位资深的硬盘健康检测专家。请分析以下硬盘在指定时间区间内的监控数据，包括容量使用变化和SMART健康信息变化：
    
    硬盘基本信息：
    {disk_info}
    
    时间区间：{start_time} 至 {end_time}
    
    容量变化数据：
    {capacity_data}
    
    SMART信息变化数据：
    {smart_data}
    
    请你：
    1. 分析容量使用变化趋势，是否存在异常增长
    2. 分析SMART各项指标的变化，指出哪些指标出现了异常
    3. 基于上述数据分析，推测硬盘当前的健康状态
    4. 给出相应的建议，包括是否需要备份数据、是否需要更换硬盘等
    
    请用中文回答，保持专业、客观，不要使用markdown格式，分段清晰。